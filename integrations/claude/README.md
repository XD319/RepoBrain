# Claude Adapter

This directory contains the minimal RepoBrain template for Claude Code.

- Primary artifact: `SKILL.md`
- Recommended inputs: `brain inject` output plus `brain suggest-skills` hints
- Durable memory source: `.brain/`

Copy `SKILL.md` into the Claude-specific skill location you use for the repo, then adjust the operational details without creating a second repo-memory model.
