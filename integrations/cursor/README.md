# Cursor Adapter

This directory contains a minimal RepoBrain rules template for Cursor.

- Primary artifact: `repobrain.mdc`
- Suggested destination: `.cursor/rules/repobrain.mdc`
- Durable memory source: `.brain/`

The template keeps Cursor aligned with RepoBrain's existing schema and outputs instead of introducing Cursor-only project memory.
