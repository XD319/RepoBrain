# RepoBrain v0.2.1

这是针对 `v0.2.0` conversation-start 功能发布的补丁版本。

## Fixed

- 补齐 `conversation-start` 实现与测试中遗漏的 Prettier 格式化修改，恢复 CI 绿灯。
- 确保 `main` 在合入 smart conversation refresh 之后重新处于可发布状态。

## Verification

- `npm run format:check`
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run smoke:package`

## Notes

- `v0.2.0` 仍然是 smart conversation refresh 的功能版本。
- `v0.2.1` 是在正式发布前补上的 CI 修复补丁版本。
