# RepoBrain v0.2.1

Patch release for the `v0.2.0` conversation-start rollout.

## Fixed

- Restore CI by applying the missing Prettier formatting updates for the `conversation-start` implementation and tests.
- Keep the `main` branch releasable after the smart conversation refresh changes landed in `v0.2.0`.

## Verification

- `npm run format:check`
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run smoke:package`

## Notes

- `v0.2.0` remains the feature release for smart conversation refresh.
- `v0.2.1` is the follow-up patch release that brings CI back to green before publishing.
